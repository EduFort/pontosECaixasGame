#include <SFML/Graphics.hpp>
#include <iostream>

int main()

{
    // Cria a janela

    sf::RenderWindow window(sf::VideoMode(400, 300), "Bola Branca - SFML");



    // Cria a "bola" (um círculo)

    sf::CircleShape circle(30.0f); // raio de 50 pixels

    circle.setFillColor(sf::Color::White);
    int xStart = 150, xUpdate = 150, yStart = 100, yUpdate = 100;
    circle.setPosition(xStart, yUpdate); // centraliza aproximadamente (janela 400x300)



    // Loop principal
    sf::Clock fpsClock;
    window.setFramerateLimit(100);
    while (window.isOpen())

    {

    	float currentTime = fpsClock.restart().asSeconds();
    	float fps = 1.0f / (currentTime);

    	std::cout << "fps: "<< fps << std::endl;

        sf::Event event;

        while (window.pollEvent(event))

        {

            if (event.type == sf::Event::Closed)

                window.close();

        }

        if((xUpdate<300)&&(yUpdate<100)){
        	circle.setPosition(xUpdate++, yUpdate++);
        } else if((xUpdate>300)&&(yUpdate<100)){
        	circle.setPosition(xUpdate--, yUpdate++);
        }else if((xUpdate<300)&&(yUpdate>100)){
        	circle.setPosition(xUpdate++, yUpdate--);
        }else if((xUpdate>300)&&(yUpdate>100)){
        	circle.setPosition(xUpdate--, yUpdate--);
        }

        window.clear(sf::Color::Blue); // fundo preto

        window.draw(circle);

        window.display();

    }



    return 0;

}
